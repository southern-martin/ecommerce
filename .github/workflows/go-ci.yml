name: Go Services CI

on:
  push:
    branches: [main, develop]
    paths:
      - 'services/**'
      - 'pkg/**'
      - 'proto/**'
      - 'go.work'
      - 'go.work.sum'
  pull_request:
    branches: [main]
    paths:
      - 'services/**'
      - 'pkg/**'
      - 'proto/**'
      - 'go.work'
      - 'go.work.sum'

env:
  GO_VERSION: '1.25'
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository }}

jobs:
  lint-and-test:
    name: Lint & Test
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      nats:
        image: nats:2.10-alpine
        ports:
          - 4222:4222
        options: >-
          --health-cmd "true"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      POSTGRES_HOST: localhost
      POSTGRES_PORT: '5432'
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      REDIS_URL: redis://localhost:6379
      NATS_URL: nats://localhost:4222

    strategy:
      fail-fast: false
      matrix:
        service:
          - auth
          - user
          - product
          - cart
          - order
          - payment
          - tax
          - shipping
          - return
          - search
          - review
          - media
          - notification
          - cms
          - promotion
          - loyalty
          - affiliate
          - chat
          - ai

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: services/${{ matrix.service }}/go.sum

      - name: Install golangci-lint
        run: go install github.com/golangci/golangci-lint/cmd/golangci-lint@v1.57.2

      - name: Lint ${{ matrix.service }}
        working-directory: services/${{ matrix.service }}
        run: golangci-lint run ./...

      - name: Test ${{ matrix.service }}
        working-directory: services/${{ matrix.service }}
        run: |
          go test ./... -race -coverprofile=coverage.out -covermode=atomic -v
        env:
          DB_NAME: test

      - name: Build ${{ matrix.service }}
        working-directory: services/${{ matrix.service }}
        run: go build -o /dev/null ./cmd/...

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.service }}
          path: services/${{ matrix.service }}/coverage.out
          retention-days: 7

  lint-and-test-pkg:
    name: Lint & Test Shared Packages
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Install golangci-lint
        run: go install github.com/golangci/golangci-lint/cmd/golangci-lint@v1.57.2

      - name: Lint shared packages
        working-directory: pkg
        run: golangci-lint run ./...

      - name: Test shared packages
        working-directory: pkg
        run: go test ./... -race -coverprofile=coverage.out -covermode=atomic -v

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage-pkg
          path: pkg/coverage.out
          retention-days: 7

  integration-test:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: [lint-and-test]

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      nats:
        image: nats:2.10-alpine
        command: ["--jetstream"]
        ports:
          - 4222:4222
        options: >-
          --health-cmd "true"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      POSTGRES_HOST: localhost
      POSTGRES_PORT: '5432'
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      REDIS_URL: localhost:6379
      NATS_URL: nats://localhost:4222

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Create test databases
        run: |
          for db in ecommerce_auth ecommerce_users ecommerce_products ecommerce_cart ecommerce_orders ecommerce_payments ecommerce_search ecommerce_reviews ecommerce_notifications ecommerce_media ecommerce_cms ecommerce_promotions ecommerce_loyalty ecommerce_affiliates ecommerce_tax ecommerce_shipping ecommerce_returns ecommerce_chat ecommerce_ai; do
            PGPASSWORD=test psql -h localhost -U test -d test -c "CREATE DATABASE $db;" 2>/dev/null || true
          done

      - name: Run integration tests
        working-directory: tests
        run: GOWORK=off go test -tags integration ./integration/... -v -timeout 5m
        env:
          DB_NAME: test

  e2e-test:
    name: E2E Tests
    runs-on: ubuntu-latest
    needs: [integration-test]
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      nats:
        image: nats:2.10-alpine
        command: ["--jetstream"]
        ports:
          - 4222:4222
        options: >-
          --health-cmd "true"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      POSTGRES_HOST: localhost
      POSTGRES_PORT: '5432'
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      REDIS_URL: localhost:6379
      NATS_URL: nats://localhost:4222

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Create test databases
        run: |
          for db in ecommerce_auth ecommerce_users ecommerce_products ecommerce_cart ecommerce_orders ecommerce_payments ecommerce_search ecommerce_reviews ecommerce_notifications ecommerce_media ecommerce_cms ecommerce_promotions ecommerce_loyalty ecommerce_affiliates ecommerce_tax ecommerce_shipping ecommerce_returns ecommerce_chat ecommerce_ai; do
            PGPASSWORD=test psql -h localhost -U test -d test -c "CREATE DATABASE $db;" 2>/dev/null || true
          done

      - name: Run E2E tests
        working-directory: tests
        run: GOWORK=off go test -tags=e2e ./e2e/... -v -timeout 10m
        env:
          DB_NAME: test

  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    needs: [lint-and-test]

    strategy:
      fail-fast: false
      matrix:
        service:
          - auth
          - user
          - product
          - cart
          - order
          - payment
          - tax
          - shipping
          - return
          - search
          - review
          - media
          - notification
          - cms
          - promotion
          - loyalty
          - affiliate
          - chat
          - ai

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: services/${{ matrix.service }}/go.sum

      - name: Install gosec
        run: go install github.com/securego/gosec/v2/cmd/gosec@latest

      - name: Run gosec on ${{ matrix.service }}
        working-directory: services/${{ matrix.service }}
        run: gosec -quiet -severity medium ./...

  build-images:
    name: Build & Push Docker Images
    needs: [lint-and-test, lint-and-test-pkg, security-scan, integration-test, e2e-test]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    strategy:
      fail-fast: false
      matrix:
        service:
          - auth
          - user
          - product
          - cart
          - order
          - payment
          - tax
          - shipping
          - return
          - search
          - review
          - media
          - notification
          - cms
          - promotion
          - loyalty
          - affiliate
          - chat
          - ai

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_PREFIX }}/${{ matrix.service }}
          tags: |
            type=sha,prefix=
            type=ref,event=branch
            type=raw,value=latest

      - name: Build and push ${{ matrix.service }}
        uses: docker/build-push-action@v5
        with:
          context: .
          file: services/${{ matrix.service }}/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Trivy vulnerability scan ${{ matrix.service }}
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.IMAGE_PREFIX }}/${{ matrix.service }}:latest
          format: table
          exit-code: '1'
          severity: 'CRITICAL,HIGH'
          ignore-unfixed: true
